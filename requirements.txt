# MCP servers
mcp>=1.0.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Image generation (Pollinations.ai)
Pillow>=10.0.0

# Content generation (Google Gemini)
google-genai>=1.0.0

# Design assets (HyperFrames / Seedance / Higgsfield)
jinja2>=3.1.0

# Telegram bot
python-telegram-bot>=21.0.0

# PDF generation (for document posting)
reportlab>=4.0.0

# Research
pytrends>=4.9.0
pandas>=2.0.0

# Scheduler
apscheduler>=3.10.0

# Web server (Cloud Run webhook mode)
fastapi>=0.115.0
uvicorn>=0.30.0

# Google auth (for ADC on Cloud Run)
google-auth>=2.0.0
